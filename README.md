# Wavegen

Simple wavetable generator helper.

Concatenates single cycle waveforms into wavetables, sorts the wave-cycles by harmonic complexity and adds spectral morphing between the frames.

Waveforms are picked randomly from a folder, also phase aligns them to the fundamental and rotates the waves so they start at zero crossing.